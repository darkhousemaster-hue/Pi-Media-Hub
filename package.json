{
  "name": "pi-media-hub",
  "version": "1.0.16",
  "description": "Raspberry Pi Media Hub - Slideshow, Music & Video Manager",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently -n SERVER,CLIENT -c bgBlue.bold,bgGreen.bold \"node server.js\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "_comment": "The Pi builds from source on every update, so everything the build needs is a real dependency. Leaving vite/react in devDependencies made `npm install` drop them whenever NODE_ENV=production (which systemd sets), and the build then failed.",
  "dependencies": {
    "@vitejs/plugin-react": "^4.0.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "socket.io": "^4.6.2",
    "socket.io-client": "^4.6.2",
    "vite": "^4.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
